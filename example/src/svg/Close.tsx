import close from "./32dp/close.svg";

const Close = () => {
    return (
        <img
            src={close}
            className="Close"
            alt="close"
            style={{ width: "100%", height: "100%" }}
        />
    );
};

export default Close;
